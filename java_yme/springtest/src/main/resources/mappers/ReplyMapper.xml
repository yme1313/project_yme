<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.green.springtest.dao.ReplyDAO">
	<insert id="insertReply">
		insert into reply(rp_me_id,rp_bd_num,rp_content)
			values(#{rvo.rp_me_id},#{rvo.rp_bd_num},#{rvo.rp_content})
	</insert>
	<select id="getReplyList" resultType="kr.green.springtest.vo.ReplyVO">
		select * from reply 
			where rp_bd_num = #{rp_bd_num} and rp_valid = 'I'
			order by rp_num desc
			limit #{cri.pageStart}, #{cri.perPageNum}
	</select>
	<select id="getTotalCount" resultType="int">
		select count(*) from reply 
			where rp_bd_num = #{rp_bd_num} and rp_valid = 'I'
	</select>
	<update id="updateReply">
		update reply set
			rp_content = #{rvo.rp_content},
			rp_upDate = now(),
			rp_valid = #{rvo.rp_valid}
			where rp_valid = 'I' and rp_num = #{rvo.rp_num}
	</update>
</mapper>